import { responsiveHeight as RH,responsiveFontSize as RFS,responsiveWidth as RW , } from "react-native-responsive-dimensions"
const colors = {
  white: '#fff',
  textInpcolor: '#CDC2C2;',
  registerbtn: '#1EB808',
};
const SIZES={
    primary:30,
    secondry:16,
    textinput:16,
    btn:20
}

export {colors,SIZES}